* Request should be named - Untitled can be default, user can change.--pending
* React app title - Rename to <your app name>--pending
* Set default content-type to app/json--completed
* Show loading near Send too--pending
* Create collection: Use Trello checklist format. Don't use alerts. Use normal HTML elements instead--done with warnings
* request - send request on enter button clicked--penidng---form fields must be in form tag
* history - latest on top--pending
* 


* GitHub issue strategy
* Use Trello API using <your app name>

* Electron App


Code:

* MainComponent - Rename to "Request"
* Create a collections.js, `requests.js`, `api.js` etc (See comments below)
* There must one source - no p1, p2, p3
* Remove unused code
* Naming - Give descriptive names - Rename Display, Main etc. Good names are Request, History, Collections, RequestHeaders etc...

requests.js

export function sendRequest(url, method, headers, body) {
    return Promise()
}

api.js

function createCollection() {
    // use firebase
}

function addRequestToCollection() {

}

function addRequestToHistory() {

}
RequestComponent {

    onSend() {
        requests.sendRequest(request)
        addRequestToHistory(request)
    }
}


history: [
    {
        'id': 'req-1',
        'headers',
        'body'
    },
    {
        'id': 'req-2',
        'headers',
        'body'
    }
]

collections: [
    {
        'id': 'adfasdf',
        'name': 'adfasdf',
        'requests': ['req_1_id', 'req_2_id']
    }
]
To Do Tasks
-> react-alerts -done
-> test cases each by each.----------completed 
-> title change-------pending
-> play button on press to loading.--tabs have been removed so no problem
-> Show errors in fetch---important and timeout -- done but timeout pending
-------------Collections name should not be same------completed
------------headers by default application/json is added

show tabs on condn --removed tabs instead response component loads on condn
history sort--done

on send --- setState toPlay to null ----done by adding sendSwitch

collection name cannot be removed-----pending
---if no testcase the json will be printed------completed
